"""真实模型评测（P1-4）的诚实记录与门控。

与 ``evals_runner``（面向行为契约的夹具/真实模型冒烟）不同，本包聚焦`真实模型`
评测的诚实核算：
  - 只把**真实网络调用**计入「模型行为通过率」；fixture/contract 测试绝不进入；
  - 无真实调用（缺凭据）时，模型行为报告显示 **0 个样本**（HOLD），绝不用夹具
    生成“满分”宣传；
  - 配置预算上限与凭据保护，支持手动/定时触发的真实评测 job。
"""
