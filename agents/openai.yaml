interface:
  display_name: "AIPD-OS Execution Backend"
  short_description: "AI 产品工程决策执行后端：state management、validation、issue tracking、readiness gate"
  default_prompt: "使用 AIPD-OS 执行后端提供的 CLI 命令或 MCP 服务来管理产品开发状态、验证计划、问题跟踪和制造就绪度评估。"
policy:
  # AIPD-OS 是执行后端，不是 agent-facing orchestrator。
  # IdeaToLaunch 是唯一的 agent-facing 入口。
  allow_implicit_invocation: false
  notes: "AIPD-OS 定位为执行后端。用户对话入口和 S0-S8 编排由 IdeaToLaunch 负责。"
